using System.Windows;

namespace UserMS.Views.ProSell.VipOff
{
    public partial class AddYiAan : BasePage
    {
        public AddYiAan()
        {
            InitializeComponent();
        }

        // 当用户导航到此页面时执行。
        private void Page_Loaded(object sender, RoutedEventArgs e)
        {this.Loaded -= Page_Loaded;
        }

    }
}
